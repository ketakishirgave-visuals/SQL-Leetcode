-- explanation:
-- left join keeps employees without bonuses
-- null bonuses are treated as eligible

select e.name,
       b.bonus
from employee e
left join bonus b
  on e.empid = b.empid
where b.bonus < 1000
   or b.bonus is null;
