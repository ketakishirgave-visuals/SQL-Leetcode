-- explanation:
-- condition is applied in the join to preserve sellers
-- null orders indicate no sales in 2020

select s.seller_id
from seller s
left join orders o
  on s.seller_id = o.seller_id
 and year(o.sale_date) = 2020
where o.order_id is null;
