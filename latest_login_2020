-- explanation:
-- max timestamp gives the most recent login
-- users outside the year are excluded

select user_id,
       max(time_stamp) as last_login
from logins
where year(time_stamp) = 2020
group by user_id;
